parse_rules:
  fields:
    name:
      source_key: "name"
      type: "str"
      required: true
    age:
      source_key: "age"
      type: "int"
      required: false

defaults:
  age: 18

ignore:
  empty_lines: true
  comment_prefix: "#"
